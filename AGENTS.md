# AGENTS.md — правила работы в проекте CloudlyRu

Действуют вместе с глобальными правилами (`~/work/AGENTS.md`, `~/.dsh/AGENTS.md`). Здесь —
только то, что специфично для этого репозитория: сборка, выкладка и проверка на телефоне.

**Единственный файл инструкций проекта — этот.** `CLAUDE.md` рядом нет намеренно: в одном
каталоге харнесс читает что-то одно (pi, Codex и DSH — `AGENTS.md`, Claude Code — `CLAUDE.md`),
и два файла неизбежно расходятся. Всё актуальное — здесь.

## Модель — qwen3.5-235b-a22b-instruct-2507

## Код без комментариев (локальный override глобального правила)

В этом проекте комментарии в `.py` / `.ts` / `.dart` / `.swift` **не пишем**. Правило
перекрывает глобальный пункт «код с комментариями» из `~/work/AGENTS.md` — только для
репозитория cloudlyru.

- **Исключение** — только если без комментария код не читается: `MATH: ...` (нестандартная
  формула), `PARSING: <чужой формат, версия>` (parсинг karoshi-данных), `SEC: <RFC/CVE>`. 
  Стаицы на файл, имя впереди.
- **Никогда не пишем**: объяснение имени/функции; `TODO` без issue-контроля; «раньше было
  X, теперь Y» (git-лог).
- **Найденные комментарии удалять с правкой**: не big-bang «удалил все», а при редактировании
  блока — вычистить комменты вокруг в том же коммите.
- **Переезд смысла**: если коммент несёт наследство (алгоритм, инвариант, ссылка на
  стандарт) — выносится в README рядом; сам коммент стирается.
- **JSDoc `@deprecated use X`** — остаётся (editor-tools).
- Полный план и переоценка очистки: [`CODING-PLAN.md`](CODING-PLAN.md).

## Обязательный порядок работы в сессии: ворктри → пуш в main → workflows

Доработка в новой сессии делается **не в основном каталоге репозитория**, а в отдельном
git worktree. Законченная работа уезжает прямо в `main` (ревью через PR в проекте нет), а
основной каталог остаётся чистым и рабочим.

1. **Ворктри от свежего main.**
   ```
   git fetch origin main
   git worktree add ../cloudlyru-<тема> -b <тема> origin/main
   ```
   Все правки, сборки и проверки — там.
2. **Собрать после правок — сломанное не пушить.** Сервер — `npx tsc --noEmit` (или
   `pnpm run build`); клиент — `flutter analyze`. Менялся Flutter — **сначала поднять номер
   сборки** `version: 1.0.0+N` в `flutter/pubspec.yaml` (N+1): приложения уходят в релиз
   автоматически, а публикация отвергает номер, который не больше уже выпущенного.
3. **Push в main** — из ворктри: `./scripts/gh-push.sh HEAD:main` (токен берётся из
   `~/work/.env`, ключ `GH_SOBOGD`; других учёток не использовать). Если `main` успел уехать —
   сначала `git fetch origin main && git rebase origin/main`, потом пуш.
4. **Дальше работают workflows — их четыре, и запускаются они по изменённым путям:**
   - `src/**`, `prisma/**`, `package.json`, `pnpm-lock.yaml`, `.env.example` → `deploy.yml`
     (сборка и деплой сервера) — отдельно его дёргать не нужно;
   - `flutter/**` → `android.yml`, `macos.yml`, `ios.yml` (три приложения).
   Менялось и то и другое — запускаются все четыре; менялись только README или инструкции —
   ни один, и это нормально.
5. **За workflow не следить и итог владельцу не докладывать.** `gh run watch`, повторные
   прогоны и разбор логов — только если он попросил об этом прямо (разово, вручную).
6. **Убрать за собой:** `git worktree remove ../cloudlyru-<тема>`.

Локальный релиз (`./scripts/build-release.sh all`) остаётся запасным путём: он быстрее
холодного раннера и умеет `--force` для пересборки того же номера. Публикуют оба пути одними
и теми же `scripts/publish-*.mjs`. Номер сборки общий для Android, macOS и iOS: подняли —
выпускайте все три (теперь это делает CI одним заходом).

Пуш делает `scripts/gh-push.sh` — он же перезапускает мост, если в коммитах менялся
`agents/pi-bridge/`. Старый `scripts/push-to-main.sh` (тоже пуш в main, но без перезапуска
моста) оставлен лежать; в новых сессиях берите `gh-push.sh`.

Mac-агенты (статус-панель, туннель, LLM, whisper, claude) живут в `agents/mac/`.
Панель слушает `127.0.0.1:18810`; её ручки
проксирует `src/mac/`, заголовок `X-Mac-Token` берётся из `MAC_SERVICE_TOKEN`. Перезапуск
агентов: `agents/mac/install.sh` или `launchctl kickstart -k gui/$(id -u)/com.agent.<label>`.

## Телефон по adb — установка сразу

- **Определять подключение самому**: `adb devices` (adb нет в `PATH`, он лежит в
  `/opt/homebrew/share/android-commandlinetools/platform-tools/adb`).
- **Телефон подключён — ставить сборку туда сразу**, не дожидаясь релиза:
  `flutter build apk --release` (подпись берётся из `flutter/android/keystore.properties`,
  ключ тот же, что у установленного приложения) и `adb install -r build/app/outputs/flutter-apk/app-release.apk`.
  Данные и настройки при этом сохраняются.
- Телефон не подключён — ничего не выдумывать: собрать APK и сказать, что его можно поставить
  по adb или взять с https://files.iq-factura.com/apk.
- Проверяет работу приложения владелец сам: сценарии через adb не водить, логи ради проверки
  не снимать — это только установка.

## Настольное приложение на маке

- Собирается CI (`macos.yml`, push по `flutter/**`) из того же `flutter/` и публикуется в
  https://files.iq-factura.com/macos. Локально — `./scripts/build-release.sh macos`
  (запасной путь, умеет `--force`).
- Установлено в `/Applications/Cloudly.app` и обновляется кнопкой в «Настройках»: приложение
  само скачивает архив, проверяет размер и sha256 и подменяет свой бандл
  (`flutter/lib/features/settings/macos_update.dart`). Ставить сборку руками не нужно.
- Сборка не подписана Developer ID и не нотаризована: своего мака это не касается (архив
  качает само приложение, карантина на нём не появляется), но передать её на чужой мак нельзя —
  Gatekeeper не пустит.
- Проверить сборку до пуша можно локально: `cd flutter && flutter build macos --release`.

## Что где лежит

- `flutter/` — мобильный клиент (Flutter, applicationId `ru.cloudly.sync`).
- `src/` — сервер (NestJS + Prisma), деплой автоматический при push в `src/**`.
- `src/sync/`, `flutter/lib/sync/` — синхронизация: связки «папка устройства ↔ папка облака»
  (`SyncLinks`), зеркало файлов (`MirrorEngine`), очередь выгрузки.
- `FLUTTER.md`, `DEPLOY.md`, `PLAN.md` — подробности по клиенту, выкладке и замыслу.

## Git Token — Universal Helper

**Всегда используйте скрипт `/Users/sobogd/work/.git-token.sh`** перед операциями с GitHub.

```bash
source /Users/sobogd/work/.git-token.sh
gh workflow run deploy.yml
```

Скрипт автоматически определяет проект и подставляет правильный токен:
- **Тангем проекты** (`/tangem/`, `/tangem-checkout-web/`) → `GH_BSOKOLOV_TANGEM`
- **Все остальные** → `GH_SOBOGD`

**Никогда не используйте `gh auth login` без токена** — это активирует дефолтную учётку
(`sobogd`), у которой токен невалиден для Tangem.

If you see authentication errors with `gh`, run the script first!
