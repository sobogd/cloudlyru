-- Абсолютный путь файла на телефоне: веб по нему просит выгрузить конкретный файл,
-- а в дереве показывает относительный путь (как он ляжет в облако).
ALTER TABLE "DeviceEntry" ADD COLUMN "localPath" TEXT;
