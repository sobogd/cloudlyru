-- Второстепенные папки письма.
--
-- Одно письмо бывает сразу в двух папках: отправленное себе (наша копия в «Исходящих» плюс
-- пришедшее обратно во «Входящие») и две копии на сервере у iCloud (INBOX и Sent Messages).
-- До этой колонки такие письма схлопывались в одну строку с одной папкой: отправленное себе
-- письмо исчезало из «Входящих» вовсе. Двумя строками решать нельзя — задвоились бы вложения
-- и запись файла в дереве, поэтому папок у письма теперь список: основная плюс дополнительные.

ALTER TABLE "MailMessage" ADD COLUMN "alsoBoxes" TEXT[] NOT NULL DEFAULT ARRAY[]::TEXT[];
