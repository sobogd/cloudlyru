-- Системная папка «Телефон» — корень зеркала папок телефона. Её id хранится у владельца:
-- по нему папку нельзя удалить, переименовать или перенести (гарды в Folders и Dav), а
-- клиент получает её готовой, а не заводит по имени сам.
ALTER TABLE "User" ADD COLUMN "phoneFolderId" TEXT;
