-- Чекпойнт распаковки: индекс последнего обработанного файла архива (в порядке
-- возрастания смещения) + счётчики на этот момент. Позволяет продолжать
-- распаковку после рестарта сервиса с места, а не с первого файла.
ALTER TABLE "UnzipJob" ADD COLUMN "cursorIndex" INTEGER NOT NULL DEFAULT 0;
