-- Стиль ответа у чата: normal | caveman | humanizer.
--
-- Подсказка, которой задаётся манера речи, собирается на сервере (`src/ai/prompts.ts`) — в
-- приложении её нет, как и ключа провайдера. Здесь хранится только выбор человека, потому что
-- он относится к конкретному разговору: «пещерный» чат не должен менять манеру соседнего.
--
-- Значение по умолчанию «normal» — это и стиль обычного ассистента, и то, чем отвечают чаты,
-- созданные до появления колонки: у них манера речи была ровно такой.

-- AlterTable
ALTER TABLE "ai_chats" ADD COLUMN     "style" TEXT NOT NULL DEFAULT 'normal';
