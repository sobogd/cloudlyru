/**
 * Промпты раздела «Чат»: сейчас здесь только сжатие длинной истории.
 *
 * Правила ответа и работа с источниками переехали в агента (`agent.service.ts`): он решает
 * сам, что искать и где, и его инструкции живут рядом с инструментами. Здесь осталось то,
 * что не зависит от поиска, — сжатие разговора в выжимку, когда история становится длинной.
 */
/**
 * Просьба сжать разговор в выжимку.
 *
 * Нужна на длинных диалогах: каждый ответ пересылает историю заново, а префилл на домашнем маке
 * идёт медленно. Выжимка заменяет старые сообщения — важное остаётся, объём падает в разы.
 */
export const COMPACT_PROMPT =
  'Сожми переписку в короткую выжимку на языке разговора. Сохрани: о чём договорились, ' +
  'названия, числа, даты, принятые решения и открытые вопросы. Убери вежливость и повторы. ' +
  'Пиши только выжимку, без вступления.';

/** Заголовок для выжимки: по нему в контексте видно, что это пересказ, а не слова человека. */
export const COMPACT_HEADER = 'Выжимка предыдущего разговора:';

/**
 * Сколько символов истории терпим, прежде чем сжимать.
 *
 * Понижено с 12 000 до 8 000 после замера: prefill на маке идёт ~200 токенов в секунду, поэтому
 * история в 12 000 символов сама по себе добавляла к каждому ответу около 20 секунд ожидания, а
 * разговор от этого не выигрывал.
 */
export const COMPACT_AFTER_CHARS = 8_000;
