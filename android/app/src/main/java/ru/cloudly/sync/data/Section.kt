package ru.cloudly.sync.data

/**
 * Разделы приложения. У каждого свой набор выбранных папок на телефоне:
 * «Файлы» показывает документы, «Фото» — фото и видео.
 */
enum class Section { FILES, PHOTOS }
