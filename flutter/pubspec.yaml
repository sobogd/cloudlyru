name: cloudly_flutter
description: "CloudlyRu mobile client (Flutter)"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
# version ниже — это versionName+versionCode релиза: сборку собирает и публикует
# scripts/build-release.sh на своём маке (workflow сборки приложений больше нет), и номер
# сборки обязан расти с каждым выпуском. Номер общий для платформ: из него же берётся
# CFBundleVersion настольной и iOS-сборки, поэтому поднимать его достаточно один раз.
version: 1.0.0+178

environment:
  sdk: ^3.13.3

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  dio: ^5.11.1
  flutter_riverpod: ^3.4.3
  shared_preferences: ^2.5.5
  intl: ^0.20.3
  dio_cookie_manager: ^3.5.0
  cookie_jar: ^4.0.9
  cached_network_image: ^4.0.0
  video_player: ^2.14.0
  flutter_map: ^8.3.2
  latlong2: ^0.10.1
  # Тело письма показываем системным WebView: письма верстают таблицами, медиазапросами и
  # inline-стилями, то есть разметкой для браузерного движка (см. lib/features/mail/mail_body_web.dart).
  webview_flutter: ^4.14.1
  # Прямая зависимость, а не транзитивная: политика смешанного контента (http-картинки в письмах)
  # настраивается только в android-реализации — `setMixedContentMode`; без явной зависимости её
  # тип не импортируется.
  webview_flutter_android: ^4.14.1
  file_picker: ^13.0.0
  open_filex: ^4.7.0
  crypto: ^3.0.7
  # Отрисовка markdown в ответах чата: модель отвечает заголовками, списками, таблицами и
  # блоками кода, и показывать это сырым текстом нечитаемо. Пакет — продолжение закрытого
  # `flutter_markdown` от Flutter-команды (у него в pubspec стоит «discontinued»), GFM из коробки.
  flutter_markdown_plus: ^1.0.12
  # Прямая зависимость, а не транзитивная: свой обработчик блока кода принимает узел разбора
  # (`md.Element`), и без явной зависимости его тип не импортируется (`depend_on_referenced_packages`).
  # Версия — та же, что тянет flutter_markdown_plus, иначе типы узлов разойдутся.
  markdown: ^7.3.1
  # Прямая зависимость, а не транзитивная: номер файла в файловой системе берётся вызовом
  # `stat(2)` из libc (`lib/sync/device/native_stat.dart`), а этот пакет даёт для FFI
  # выделение памяти и C-строки (`calloc`, `toNativeUtf8`).
  ffi: ^2.2.0
  path_provider: ^2.1.6
  url_launcher: ^6.3.2
  package_info_plus: ^10.2.1
  sqflite: ^2.4.4
  path: ^1.9.1
  flutter_secure_storage: ^11.1.1
  webview_flutter_wkwebview: ^3.26.1
  record: ^7.1.1

dev_dependencies:
  # Тестов в проекте нет намеренно (решение владельца): `flutter_test` и каталог `test/`
  # удалены, шаг `flutter test` убран из `.github/workflows/android.yml`. Остаётся
  # `flutter analyze` — он в проекте обязателен и должен быть чистым.
  #
  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Генераторов кода здесь нет намеренно: модели (`lib/api/models.dart`) написаны руками,
  # и `freezed`/`json_serializable` были объявлены, но не использовались ни одним файлом —
  # ни одного `part '*.g.dart'`, ни одной аннотации. Мёртвые зависимости убраны, чтобы
  # `pubspec.yaml` не обещал того, чего в коде нет (в `FLUTTER.md` строка про
  # «freezed + json_serializable» осталась — её правит владелец документа).
  #
  # Если генераторы всё же понадобятся, порядок такой: сначала `json_serializable`
  # с конвертерами (число строкой, отсутствующее обязательное поле — сейчас хелперы `JsonX`
  # подставляют дефолты, а генератор по умолчанию на этом упадёт), потом `freezed` ради
  # `copyWith`/`==`. Вместе с `build_runner`.

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
